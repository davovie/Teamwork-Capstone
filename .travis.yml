
language: node_js
node_js: 
  - "stable"
cache:
  directories:
    - "node_modules"
after_success: npm run coverage
before_script:
  - psql -c "CREATE USER david WITH PASSWORD 'password';" -U postgres
  - psql -c 'create database teamwork;' -U david
  
services:
  - postgresql